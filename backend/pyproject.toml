[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"

[tool.coverage.run]
source = ["."]
omit = [
    "tests/*",
    "config.py",
    "database.py",
    "*/__init__.py"
]

[tool.coverage.report]
show_missing = true
fail_under = 5
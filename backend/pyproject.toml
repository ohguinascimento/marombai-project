[tool.pytest.ini_options]
testpaths = ["tests", "../.husky"]
python_files = "test_*.py"

[tool.coverage.run]
source = ["."]
omit = [
    "tests/*",
    "*/__init__.py",
    "config.py",
    "database.py"
]

[tool.coverage.report]
show_missing = true
fail_under = 80